
# Runpod — REST control plane (https://console.runpod.io/user/settings)
RUNPOD_API_KEY=

# S3 — network-volume bucket push/pull: the default persistence path (and the VANISHED-pod
# fallback). Storage-scoped, separate from RUNPOD_API_KEY. Required unless -NoPersist.
S3_ACCESS_KEY=
S3_SECRET=
