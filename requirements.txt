# OME-GAUGE dependencies.
#
# The experiment splits into a CPU half (directions, steered activations, NLA-free detectors, and
# all analysis — runs locally, free) and a GPU-pod half (the NLA AV/AR round-trip, generation,
# and the LoRA fine-tune arm — paid, gated). Only the CPU half is required to reproduce the
# committed analyses; the pod extras are listed but commented.

# --- local CPU (directions / steer / detectors / analysis / figures) ---
numpy>=1.26
pyarrow>=15
pyyaml>=6.0
matplotlib>=3.8        # figures (optional; the analysis JSON is the deliverable)
boto3>=1.34            # S3 sync of large arrays (src/s3_io.py); optional for a purely local run

# --- GPU pod only (NLA serving + round-trip; generation; fine-tuning) ---
# sglang[all]==0.5.6   # pin exactly — see .podref/sglang_fix_recipe.txt (stock >=0.5.6 pulls a broken stack)
# torch
# transformers==4.46.3 # the SFTConfig API the fine-tune arm targets
# trl==0.12.2          # LoRA SFT (Stage-3 fine-tune arm)
# peft                 # LoRA adapters
# safetensors
# huggingface_hub      # checkpoint download + av_rev provenance
# httpx                # sglang client transport (used by nla_inference)
# orjson               # numpy-aware JSON for input_embeds payloads
# Plus: git clone https://github.com/kitft/nla-inference   (provides nla_inference.py: NLAClient/NLACritic)
# Plus: the `claude` CLI on PATH for the `claude -p` judge (run locally from a neutral cwd).
