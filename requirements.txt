# NLA-final dependencies.
# Local (Phase A steer-sweep + Phase D analysis + S3 I/O): numpy, pyarrow, boto3 [+ matplotlib for figures].
# GPU pod (Phases B-F: NLA round-trip + verbalization) additionally needs the sglang/torch stack.

# --- local + pod ---
numpy>=1.26
pyarrow>=15
boto3>=1.34
pyyaml>=6.0
matplotlib>=3.8        # Phase D figures (optional; analysis.json is the deliverable)

# --- GPU pod only (NLA serving + round-trip) ---
# sglang[all]>=0.5.6
# torch
# transformers
# safetensors
# huggingface_hub      # checkpoint download + av_rev provenance
# httpx                # sglang client transport (used by nla-inference)
# orjson               # numpy-aware JSON for input_embeds payloads
# Plus: git clone https://github.com/kitft/nla-inference  (provides nla_inference.py: NLAClient/NLACritic)
