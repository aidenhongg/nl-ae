"""graphing/ — figures from the concluded NLA-final experiments.

Read-only over out/ (the frozen experiment data). Each graph is its own module,
runnable individually, e.g.:  python -m graphing.graph1_probe_accuracy
Outputs land in graphing/figures/.  python -m graphing.make_all runs them all.
"""
